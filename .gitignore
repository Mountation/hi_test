# 依赖库（npm/yarn/pnpm 安装的包，体积大且可通过 package.json 重新安装）
node_modules/
.pnp/
.pnp.js

# 构建产物（打包后的文件，可通过构建命令重新生成）
dist/
build/
out/
dist-ssr/
*.local

# 环境配置文件（可能包含敏感信息，如 API 密钥、数据库密码）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 日志和缓存文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.DS_Store  # Mac 系统自动生成的隐藏文件
Thumbs.db  # Windows 系统自动生成的缩略图缓存

# 编辑器/IDE 配置文件（如 VS Code 的 .vscode，避免团队成员的本地配置冲突）
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?  # 临时文件（如 Vim 的 .swp 缓存）